[project]
name = "globallivestreammetadata"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">= 3.14"
dependencies = [
    "humanize",
    "msgpack",
    "aiohttp",
    "aiomqtt",
]
[dependency-groups]
dev = [
    "pytest-aiohttp",
]
